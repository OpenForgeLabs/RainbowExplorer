{
  "plugins": [
    {
      "id": "redis",
      "name": "Redis",
      "baseUrl": "http://localhost:4011",
      "mountPath": "/plugins/redis",
      "defaultPath": "/",
      "description": "Explore Redis keys, values, and server metrics.",
      "enabled": true
    }
  ]
}